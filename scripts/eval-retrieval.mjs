#!/usr/bin/env node
// Runs the golden Q&A set (evals/golden.json) against retrieval and prints
// hit@1 / hit@3 / hit@8 / MRR for a few configs (vector-only vs. hybrid,
// chunk sizes, with/without heading breadcrumbs). Lands in Phase 8, once
// retrieval (Phase 5) exists to evaluate. Phase 0 placeholder for now.

console.log(
  "[eval-retrieval] Not implemented yet — retrieval lands in Phase 5, this harness in Phase 8.",
);
