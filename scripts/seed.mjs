#!/usr/bin/env node
// Creates a demo user (demo@example.com / demo-password-123) and a handful
// of sample documents through the API, so they go through the real indexing
// pipeline. Lands in Phase 3 (documents CRUD) / Phase 4 (indexing) — this is
// a Phase 0 placeholder so `pnpm seed` and `pnpm setup` have something to
// call without crashing.

console.log(
  "[seed] Not implemented yet — the documents API and indexing pipeline land in Phase 3/4.",
);
